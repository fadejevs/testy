import { createTheme } from '@mui/material/styles';

// Create a theme instance with proper shadow definitions
const theme = createTheme();

// No need to manually define shadows - MUI has defaults for elevations 0-24
// Just use the default theme which already has all shadow levels defined

export default theme; 